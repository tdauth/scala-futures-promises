package tdauth.futuresandpromises.benchmarks

import org.scalameter.api._

object Fibonacci extends Bench.LocalTime {

  performance of "Range" in {
    // nested tests
  }

  private def fibonacci(n : Int) = {
if (n == 0 || n == 1) {
  return 1
}
  }
}